name: uva_libero10_dino_jepa_minimal_dino_only

defaults:
  - uva_libero10_dino_jepa_minimal
  - _self_

model:
  policy:
    dual_teacher_params:
      enable_dino: true
      enable_jepa: false

logging:
  name: train_uva_libero10_dino_jepa_minimal_dino_only
  tags:
    - libero10
    - small_student
    - dino_spatial
    - dino_only
